import setuptools

setuptools.setup(name='banker',
      version='0.0.1',
      description='how to test',
      author='Ela course',
      install_requires = ["pyodbc"],
      packages=setuptools.find_packages(),
     )
